{
  "name": "@platform/scheduler",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings=0",
    "lint:fix": "eslint . --fix",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@platform/config": "workspace:*",
    "@platform/db": "workspace:*",
    "@platform/logger": "workspace:*",
    "@platform/teams": "workspace:*",
    "cron-parser": "^5.10.0",
    "cronstrue": "^2.51.0",
    "drizzle-orm": "^0.45.2",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@platform/tsconfig": "workspace:*",
    "@types/node": "^25.7.0",
    "tsx": "^4.22.4",
    "typescript": "^5.5.0",
    "vitest": "^4.1.10"
  }
}
