# CLA workflow test

Throwaway file to trigger the CLA Assistant GitHub Action on a real PR and confirm it comments
and reports a status check. Safe to delete — not referenced from anywhere. Opened and closed by
the same session that added `.github/workflows/cla.yml` (#655), for verification only.
